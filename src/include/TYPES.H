#ifndef TYPES_H
#define TYPES_H

#include <osbind.h>
#include <stdio.h>
#include <stdlib.h>

/* unsgined types */
typedef unsigned char UINT8;
typedef unsigned int UINT16;
typedef unsigned long UINT32;

typedef enum
{
    black = 0xff,
    white = 0x00
} colors;

#endif
